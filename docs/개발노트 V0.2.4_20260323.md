# Aitty v0.2.4 — 개발 진행 상황 보고

**작성일**: 2026-03-23
**이전 버전**: v0.2.3 (`7a6a370`, 2026-03-22)
**작업 기반**: aitty2 → aitty_v3 클론 후 개선

---

## 📊 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | Aitty (SSH + AI 터미널) |
| **유형** | WPF 데스크톱 앱 (Windows 전용) |
| **버전** | v0.2.4 |
| **저장소** | github.com/2hryul2/aitty2 → aitty_v3 (신규 디렉토리) |
| **작업 범위** | v0.2.3 P3 미착수 항목 처리 + 버전 표시 동적화 |

---

## ✅ 구현 완료 항목

### 1. 헤더 버전 동적 표시

**문제**: 헤더에 `v0.1.0` 하드코딩 → 실제 버전과 불일치

**수정**:

| 파일 | 변경 |
|------|------|
| `Aitty.csproj` | `<Version>0.2.0</Version>` → `0.2.3` (Assembly/File 동기화) |
| `App.tsx` | 하드코딩 제거 → `app:version` IPC 호출로 C# 어셈블리 버전 동적 표시 |

```tsx
// 변경 전
<span className="subtitle">(SSH + AI Terminal for Windows, v0.1.0)</span>

// 변경 후
const [appVersion, setAppVersion] = useState<string>('')
// initializeApp에서 appBridge.version() 호출
<span className="subtitle">(SSH + AI Terminal for Windows{appVersion ? `, v${appVersion}` : ''})</span>
```

**효과**: csproj `<Version>` 값만 변경하면 UI 헤더에 자동 반영

---

### 2. P3-26 — 버전·경로 정합성 수정

| 파일 | 변경 내용 |
|------|----------|
| `package.json` | `"version": "0.2.0"` → `"0.2.3"` |
| `README.md` | 타이틀에 `v0.2.3` 추가 |
| `README.md` | 경로 `D:\source\aitty2` → `D:\source\aitty_v3` (4곳) |
| `README.md` | AI Features 섹션: Claude 단독 → 4개 프로바이더 + 세션/보안 기능 반영 |

---

### 3. P3-24 — CSS 버튼 스타일 중복 제거

**파일**: `webapp/src/styles/terminal.css`

**변경 전**:
```css
/* 2곳에 동일 스타일 분산 */
.llm-settings-panel button, .connect-btn { background: #0a2a0a; border-color: #0f0; }
.terminal-controls button.active { border-color: #0f0; background: #0a2a0a; }  /* 중복 */
```

**변경 후**:
```css
/* green-button 셀렉터 통합 */
.llm-settings-panel button,
.connect-btn,
.terminal-controls button.active {
  background-color: #0a2a0a;
  color: #0f0;
  border-color: #0f0;
  font-weight: 600;
}
```

중복된 `.terminal-controls button.active` 블록(L205-208) 삭제.

---

### 4. P3-23 — SSH 개인키 파일 선택 다이얼로그

**현황**: 드롭다운(`keys.list()`)만 있었고, OS 네이티브 파일 피커 미구현

**구현 3계층:**

| 계층 | 파일 | 작업 |
|------|------|------|
| C# IPC | `Ipc/IpcHandler.cs` | `keys:browse` 핸들러 → WPF `OpenFileDialog` |
| IPC Bridge | `bridge/ipcBridge.ts` | `keys.browse()` 메서드 추가 |
| UI | `components/SSHTerminal.tsx` | `[Browse...]` 버튼 + 키 검증 경고 |

**C# 핸들러**:
```csharp
private async Task<object> HandleKeysBrowse()
{
    var result = await _webView.Dispatcher.InvokeAsync(() =>
    {
        var dialog = new Microsoft.Win32.OpenFileDialog
        {
            Title = "Select SSH Private Key",
            Filter = "All Files (*.*)|*.*|PEM Files (*.pem)|*.pem|PPK Files (*.ppk)|*.ppk",
            InitialDirectory = _keyManagerService.GetKeyDirectory(),
        };
        return dialog.ShowDialog() == true ? dialog.FileName : null;
    });

    if (result is null)
        return new { selected = false, path = (string?)null, valid = false };

    var valid = await _keyManagerService.IsValidKeyFileAsync(result);
    return new { selected = true, path = result, valid };
}
```

**동작 흐름**:
```
[Browse...] 클릭 → IPC keys:browse
→ WPF OpenFileDialog (초기 경로: ~/.ssh/)
→ 파일 선택 → keys.validate() 자동 검증
→ 유효: 드롭다운에 추가 + 선택
→ 무효: 터미널에 경고 메시지 출력
```

---

## ⏭️ P3 항목 최종 상태

| # | 항목 | v0.2.3 | v0.2.4 |
|---|------|--------|--------|
| P3-21 | services/ 삭제 + @services 별칭 제거 | ✅ (이미 없음) | — |
| P3-22 | SkipReactBuild 조건 | ✅ | — |
| P3-23 | SSH 키 파일 다이얼로그 | ❌ | ✅ |
| P3-24 | CSS 버튼 중복 제거 | ❌ | ✅ |
| P3-25 | win-x64.pubxml | ✅ (이미 존재) | — |
| P3-26 | 버전 정합성 | ❌ | ✅ |

**P3 항목 6/6 완료. 미착수 항목 없음.**

---

## 📂 변경된 파일 목록

### C# (Backend)

| 파일 | 변경 내용 |
|------|----------|
| `Aitty.csproj` | Version/AssemblyVersion/FileVersion → 0.2.3 |
| `Ipc/IpcHandler.cs` | `keys:browse` 핸들러 추가 (OpenFileDialog + validate) |

### TypeScript (Frontend)

| 파일 | 변경 내용 |
|------|----------|
| `App.tsx` | `appBridge.version()` IPC 호출, 동적 버전 표시 |
| `bridge/ipcBridge.ts` | `keys.browse()` 메서드 추가 |
| `components/SSHTerminal.tsx` | `[Browse...]` 버튼, 키 선택 후 검증 + 드롭다운 반영 |
| `styles/terminal.css` | green-button 셀렉터 통합, 중복 블록 삭제 |

### 문서

| 파일 | 변경 내용 |
|------|----------|
| `package.json` | version 0.2.0 → 0.2.3 |
| `README.md` | 버전 추가, 경로 수정(×4), AI Features 갱신 |

---

## 🔧 빌드 & 배포

```bash
dotnet publish src/Aitty/Aitty.csproj -c Release -r win-x64 \
  --self-contained true -p:PublishSingleFile=true -o publish
```

**산출물**:
```
publish/
├── Aitty.exe          (153MB, self-contained .NET 8)
└── wwwroot/
    ├── index.html
    └── assets/
        ├── index-CVZklnTf.js   (~518KB)
        └── index-4fPlctw8.css  (~8.7KB)
```

**빌드 경고**: CS0067 `RelayCommand.CanExecuteChanged` — pre-existing, 무해

---

*작성자: Claude (AI Agent)*
*최종 검토: 2026-03-23*
